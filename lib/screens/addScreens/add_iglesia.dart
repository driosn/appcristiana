import 'package:flutter/material.dart';

class AddIglesia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Añadir una Iglesia'),
        backgroundColor: Colors.grey,
      ),
    );
  }
}